const $=id=>document.getElementById(id);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function time(value){if(!value)return 'no timestamp';const date=new Date(value);return Number.isNaN(date.getTime())?'unknown time':date.toLocaleString();}
async function refresh(){try{const response=await fetch('/api/snapshot?diagnostics='+$('diagnostics').checked);if(!response.ok)throw Error();const p=await response.json();$('health').textContent=p.health;$('health').className=p.online?'online':'';$('summary').className='summary';$('summary').innerHTML=[['STATE',p.state],['ACTIVE',p.activeCount],['RUNNING',p.runningCount],['WAITING',p.waitingCount],['DONE',p.doneCount],['DEVICE',p.device.connectionState],['RGB',p.rgb.enabled?'ON':'OFF']].map(x=>`<div class="metric"><small>${esc(x[0])}</small><b>${esc(x[1])}</b></div>`).join('');$('threads').innerHTML=p.threads.map(t=>`<article class="thread"><strong>${esc(t.state)}</strong><span title="${esc(t.threadId)}">${esc(t.threadId)}</span><span title="${esc(t.workingDirectory||'')}">${esc(t.workingDirectory||'no directory')}</span><span>${esc(t.classification)}${t.focusHint==='Focused'?' · FOCUSED':''}</span><span>${esc(time(t.lastObservedUtc))}</span></article>`).join('')||'<p class="muted">No Runtime-provided user threads.</p>'}catch{$('health').textContent='RUNTIME OFFLINE';$('health').className='';$('summary').innerHTML='<p class="muted">Runtime disconnected; reconnecting…</p>'}}
$('diagnostics').onchange=refresh;refresh();setInterval(refresh,700);
